# Lobster-Tracer 本地环境变量示例
# 复制为 .env 后填入( .env 已被 .gitignore 忽略,不会提交 )
# 启动前加载: set -a; . ./.env; set +a

# 写接口 / /proxy / WebSocket 的鉴权密钥。公网部署必设,本地留空则放行。
ADMIN_TOKEN=

# WebSocket 仅允许的来源(防跨站订阅)。留空则本地放行。
WS_ALLOWED_ORIGIN=
