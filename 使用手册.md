# 🦞 Lobster-Tracer 作品使用手册

> 外滩大会全民黑客松 · AI Coding 大赛参赛作品  
> 版本 **v0.5.13** ｜ 更新时间 2026-07-28 ｜ 部署平台 Railway

---

## 一、作品是什么

**Lobster-Tracer 是一个 AI Agent 可观测性引擎。**

一句话定位：让多 Agent 协作的每一步决策透明可追溯 —— 可视化 · 异常检测 · 状态机建模 · 实时推送。

> Qoder 让 Agent 能做事，Lobster-Tracer 让你看清 Agent 在做什么。

把一次"让 AI 写 3000 字小说""生成季度报告""多 Agent 协作长任务"当成一次**会话**，Lobster-Tracer 会：

- 实时抓取每一次 LLM 调用的 prompt、产出、token 消耗；
- 把长任务的**状态机迁移**（大纲 → 章节生成 → 校验 → 完成）画成 Sankey 流程图；
- 自动检测**卡死信号**（状态机 self-loop、stream 断流、上游超时）；
- 把多个会话聚合成一张**可观测面板**（Fleet 视角：哪个会话烧 token 最多、哪个卡在哪一阶段）。

---

## 二、它解决什么痛点

| 痛点 | Lobster-Tracer 怎么做 |
|---|---|
| AI 长任务过程黑盒，调试只能 `printf` / 翻日志 | 会话级回放：点开任意会话即看完整 prompt + 产出 |
| 卡死无信号：状态机 self-loop、stream 断流、上游超时，事后难定位 | Sankey 可视化 + 自环检测，把"反复生成同一章"画成明显的回环 |
| 多会话难观测：不知道哪个 token 烧最多、哪个卡在哪一 phase | 聚合面板：总 token / 失败率 / 各模型消耗 / 自环 Top / 会话排行 |
| 调试面板要不停刷新 | WebSocket 实时推送，落库即广播，免轮询即时刷新 |

---

## 三、公网体验链接（直接可用，无需注册）

**调试面板**：https://lobster-tracer-production.up.railway.app/dashboard.html

打开即是满血 demo：3 个示例会话 + 真实状态机迁移 + 聚合面板，浏览器直接体验全部核心功能。

> 如偶发空白：Railway 重启/重部署后数据库会自动重新灌入示例数据（约 1 秒），刷新页面即可。

---

## 四、30 秒体验指南（评审最快路线）

1. 打开 dashboard 链接（免注册免登录，首访冷启动约 30-60s，有遮罩提示）。
2. 看下方 **Sankey 状态机图**：`chapter_gen → chapter_gen` 的回环 = "Agent 卡死"可视化（节点带中文对照）。
3. 左侧点「**AI 编辑部：三个 Agent 接力写文章**」→ 右侧「阶段迁移时间线」看 3 个 Agent 接力，每步标注 Agent + 模型（绿/黄徽章）。
4. 点「**写作 Agent 卡壳实录：大纲被打回 3 次**」→ 看自环回环 + 聚合面板自环 Top。
5. 右上角「实时」灯亮 = WebSocket 已连接，新事件落库即刷新。

### 进阶：3 分钟跑通全部核心功能

1. **打开面板** → 访问上面的 dashboard 链接。自动加载 8 个示例会话（3 个通用 demo：已完成 / 进行中 / 失败 + 5 个 Agent 端到端 session：「写一篇 3000 字 AI 文章（顺畅完成）」/「写作 Agent 卡壳实录：大纲被打回 3 次」/「AI 编辑部：三个 Agent 接力写文章」/「代码审查 Agent：自动 Review 并修 Bug」/「AI 旅行规划师：帮我安排 5 天东京游」）。「代码审查 Agent」是**非写作场景** demo（点开可见 `init → analyze → review → fix → done`）；「AI 旅行规划师」是**生活类** demo（点开可见 `init → 问偏好 → 搜目的地 → 排行程 → 算预算 → 重排自环 → done`），证明可观测性引擎不限于长文，连规划旅行都能看清每一步。
2. **看会话回放** → 点任意会话，右侧展示完整 `prompt` + `response` 明文（调试用）。
3. **看状态机** → 面板下方 Sankey 图展示 `init → outline → … → done` 全流程，特别画出 **self-loop**（`chapter_gen → chapter_gen` 卡死信号）与 **error 恢复**路径。
4. **看聚合分析** → 聚合面板含：KPI（总会话数 / 失败率 / 总 token / 平均耗时）、各模型 token 消耗、状态机自环 Top、Top 会话 token 排行。
5. **实时推送** → 面板右上"实时"指示灯亮起，表示 WebSocket 已连接；有新的迁移/事件落入时面板即时刷新，无需手动刷新。
6. **Playground 测试**（可选）→ 访问 `/playground.html` 可手动构造一次 Stream Proxy 请求（需自备上游 Key，demo 环境仅供查看示例会话）。

---

## 五、测试账号 / Key

| 场景 | 账号 / Key | 说明 |
|---|---|---|
| **公开 demo 实例** | **无需账号、无需 Token** | 直接访问即匿名可用，所有读接口开放 |
| 生产部署 | 设 `ADMIN_TOKEN` 环境变量 | 写接口 + WebSocket 强制 Bearer / `?token=` 校验 |

公开 demo 实例下，写接口（`/proxy`、`/analytics/transition`、`/analytics/seed` 等）因未设 `ADMIN_TOKEN` 同样放行，可自由探索 demo 数据；生产环境设 `ADMIN_TOKEN` 后立即收口为鉴权模式。

---

## 六、核心功能

- **会话存储与回放**：每次 LLM 调用落库，支持点开查看 prompt / response 明文。
- **OpenAI 兼容 Stream Proxy**：用 undici 自写（不接 SDK），SSE 跨包缓冲 + 真实 token 统计，带 `metadata.phase` 自动落状态机迁移。
- **状态机可视化**：ECharts Sankey，画出长任务的阶段迁移与异常（self-loop / error）。
- **异常检测**：断流 / 超时 / 重复 / 自环卡死，状态机里专门标注。
- **多会话聚合分析**（Fleet 可观测性）：跨会话 token 汇总、各模型消耗、自环卡死信号、Top 会话排行。
- **WebSocket 实时推送**：落库即广播，面板免轮询即时刷新。

---

## 七、技术栈

| 层 | 技术 |
|---|---|
| 后端 | Node.js 20 + Express + better-sqlite3（WAL 模式） |
| 前端 | 原生 HTML + ECharts 5（Sankey 状态机 + chunk 字符流时间线） |
| 代理 | undici 自写 OpenAI 兼容 Stream Proxy（SSE 跨包缓冲 + 真实 token 统计） |
| 实时 | ws（WebSocket，挂在 HTTP 同端口） |
| 部署 | Railway |

---

## 八、安全加固（已通过多轮审计闭环）

为评审呈现本项目在安全上的投入——D9–D12 连续 4 个版本，对照 3 份独立安全审计报告修复了 **9/10** 个漏洞，并补齐冒烟测试：

| # | 加固项 | 做法 |
|---|---|---|
| 1 | XSS 全转义 | 前端所有 `innerHTML` 拼接经 `escapeHtml`，错误提示不再拼接用户可控内容 |
| 2 | 认证/授权 | `requireToken` 中间件校验 `ADMIN_TOKEN`；未设则本地放行，生产强制 |
| 3 | 错误信息脱敏 | 上游/内部错误返回通用消息，不泄露部署细节 |
| 4 | 频率限制 | 内存固定窗口：proxy 20/min（防烧上游配额）、其余 120/min |
| 5 | WebSocket 安全 | 连接数上限 + origin 校验 + token 校验 + 心跳，四层防护 |
| 6 | 输入校验 | phase 白名单校验、`limit` clamp 到 [1,200] 防全表扫描 |
| 7 | 异常不再静默 | DB 写入失败由静默吞掉改为服务端日志上报 |
| 8 | body 大小限制 | JSON body 10mb → 1mb |
| 9 | 冒烟测试 | `test/smoke.mjs` 子进程起服，校验健康/聚合/状态机/会话 + 写接口 401/200 鉴权 |

> 唯一未修复项（数据库明文存储）需架构级调整（加密扩展），列为赛后工程债。

---

## 九、本地运行（开发者）

```bash
npm install
npm start
# 访问 http://localhost:3000/dashboard.html

npm test   # 运行 test/smoke.mjs 冒烟测试（子进程起服，跑完清理临时库）
```

- 本地默认不设 `ADMIN_TOKEN` 与 `DEMO_MODE`，启动为空库（冒烟测试不受影响）。
- 想本地看满血 demo：启动时设 `DEMO_MODE=1` 即可自动灌示例数据。

---

## 十、环境变量配置（部署说明）

| 变量 | 作用 | 公开 demo | 生产部署 |
|---|---|---|---|
| `ADMIN_TOKEN` | 写接口 + WebSocket 的 Bearer / `?token=` 校验密钥 | **不设置**（实例公开） | **必设**强随机串 |
| `DEMO_MODE` | 启动自动灌示例数据（Railway ephemeral 重启自愈） | 设 `1` | 不设 |
| `WS_ALLOWED_ORIGIN` | WebSocket 仅允许的来源，防跨站订阅 | 可选 | 建议设为前端域名 |
| `PORT` | 服务端口 | Railway 自动注入 | Railway 自动注入 |
| `DB_PATH` | SQLite 文件路径 | 可选（默认 `data/lobster-tracer.db`） | 可选 |

**公开 demo 实例的一份最小配置**：`DEMO_MODE=1`，其余留空。

---

## 十一、项目结构

```
lobster-tracer/
├── src/
│   ├── server.js      # Express 主服务 + 路由 + 鉴权/限流中间件
│   ├── proxy.js       # undici 自写 OpenAI 兼容 Stream Proxy
│   ├── db.js          # better-sqlite3 存储 + 聚合查询
│   └── realtime.js    # WebSocket 实时推送
├── public/
│   ├── dashboard.html # 可视化调试面板（Sankey + 聚合 + 回放）
│   └── playground.html# Stream Proxy 测试 / 会话历史
├── test/
│   └── smoke.mjs      # 冒烟测试
├── 使用手册.md         # 本文件
├── README.md
├── LICENSE            # MIT
└── package.json
```

---

## 十二、版本与路线

当前 **v0.5.13**，D1–D20 已完成；D16 为 Qoder 赛道做 demo 叙事增强（3 个长文 Agent 端到端 session + 评审引导 + 冷启动遮罩）；D17 完成审计残留清零（playground 鉴权透传、限流防 XFF 伪造、CSP 安全头、非流式超时闭环、ECharts 本地化）；D18 完成叙事重构（定位改"AI Agent 可观测性引擎"+ demo 生活化命名 + 面板引导与 phase 中文对照 + 30 秒体验指南）；D19 完成提交前收尾（加非写作代码审查 demo + C3 记忆叙事补强 + Qoder 集成说明 + 文档同步）；D20 加生活类 demo（AI 旅行规划师，含预算超支重排自环），展示对非开发者场景的通用性；提交黑客松 **2026-08-09**。

完整开发甘特图见 [README.md](./README.md)。

---

*本手册为公网可访问的作品使用手册，符合赛事"有效参赛定义"要求。*
